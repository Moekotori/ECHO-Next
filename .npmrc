# pnpm configuration for ECHO Next
# Electron requires a flat node_modules layout so the renderer can find
# modules at runtime. shamefully-hoist=true makes pnpm behave like npm.
shamefully-hoist=true

# Disable strict peer dependencies — electron-vite and friends bring
# peer-dep combinations that pnpm's strict mode rejects.
strict-peer-dependencies=false
